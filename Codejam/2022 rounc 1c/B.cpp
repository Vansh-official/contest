#include <iostream>
#include <string>
#include <vector>
#include <set>
#include <map>
#include <utility>
#include <algorithm>
#include <math.h>
#include <numeric>
#include <climits>
#include <queue>

using namespace std;

int n, m, i, j;

void printans(int t, int ans)
{
    cout << "Case #" << t << ": " << ans << endl;
}

void solve(int T)
{
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    for (int p = 1; p <= t; p++)
    {
        solve(p);
    }
}
